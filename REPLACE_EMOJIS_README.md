# 🔧 أداة استبدال الإيموجي بأيقونات CoreUI

أداة CLI لفحص واستبدال الإيموجي بأيقونات CoreUI في جميع ملفات Vue.js.

## 📋 الميزات

- ✅ فحص جميع ملفات Vue.js تلقائياً
- ✅ استبدال الإيموجي بأيقونات CoreUI المناسبة
- ✅ إضافة استيراد CIcon تلقائياً عند الحاجة
- ✅ وضع المسح (dry-run) لعرض التغييرات قبل التطبيق
- ✅ معالجة ملف واحد أو جميع الملفات

## 🚀 الاستخدام

### 1. فحص الملفات (عرض فقط - لا تغييرات)

```bash
php replace-emojis-cli.php --scan
```

هذا الأمر سيعرض جميع الإيموجي الموجودة في ملفات Vue.js دون إجراء أي تغييرات.

### 2. استبدال الإيموجي في جميع الملفات

```bash
php replace-emojis-cli.php --replace
```

⚠️ **تحذير**: هذا الأمر سيستبدل جميع الإيموجي في جميع ملفات Vue.js. تأكد من عمل نسخة احتياطية أولاً!

### 3. معالجة ملف واحد

```bash
# فحص ملف واحد
php replace-emojis-cli.php --file=assets/src/views/Dashboard.vue --scan

# استبدال في ملف واحد
php replace-emojis-cli.php --file=assets/src/views/Dashboard.vue --replace
```

### 4. عرض المساعدة

```bash
php replace-emojis-cli.php --help
```

## 📊 أمثلة على الاستبدال

| الإيموجي | أيقونة CoreUI | الاستخدام |
|---------|---------------|-----------|
| ✅ | `cil-check-circle` | النجاح، التأكيد |
| ❌ | `cil-x-circle` | الخطأ، الإلغاء |
| ⚠️ | `cil-warning` | التحذير |
| ⭐ | `cil-star` | التقييم، المميز |
| ➕ | `cil-plus` | الإضافة |
| ✏️ | `cil-pencil` | التعديل |
| 🗑️ | `cil-trash` | الحذف |
| 📊 | `cil-chart-line` | الرسوم البيانية |
| 🔄 | `cil-reload` | التحديث |
| 👤 | `cil-user` | المستخدم |

## 🔄 مثال على الاستبدال

**قبل:**
```vue
<template>
  <div>
    <p>✅ تم الحفظ بنجاح</p>
    <button>➕ إضافة جديد</button>
  </div>
</template>
```

**بعد:**
```vue
<template>
  <div>
    <p><CIcon icon="cil-check-circle" class="me-1" /> تم الحفظ بنجاح</p>
    <button><CIcon icon="cil-plus" class="me-1" /> إضافة جديد</button>
  </div>
</template>

<script setup>
import { CIcon } from '@coreui/icons-vue';
// ... باقي الاستيرادات
</script>
```

## ⚙️ آلية العمل

1. **البحث**: تفحص الأداة جميع ملفات `.vue` في مجلد `assets/src/`
2. **الكشف**: تبحث عن الإيموجي المعرفة في قائمة `$emojiMap`
3. **الاستبدال**: تستبدل الإيموجي بـ `<CIcon icon="cil-xxx" class="me-1" />`
4. **الاستيراد**: تضيف `import { CIcon } from '@coreui/icons-vue';` تلقائياً إذا لم يكن موجوداً

## 📝 ملاحظات مهمة

1. **نسخة احتياطية**: دائماً اعمل نسخة احتياطية قبل تشغيل `--replace`
   ```bash
   git add .
   git commit -m "Backup before emoji replacement"
   ```

2. **التحقق**: استخدم `--scan` أولاً لمعرفة ما سيتم تغييره

3. **إعادة البناء**: بعد الاستبدال، يجب إعادة بناء الأصول:
   ```bash
   cd assets && npm run build
   ```

4. **المراجعة**: راجع التغييرات قبل الـ commit:
   ```bash
   git diff
   ```

## 🐛 استكشاف الأخطاء

### المشكلة: لا يتم العثور على الملفات
**الحل**: تأكد من أنك في مجلد الـ plugin:
```bash
cd /path/to/wp-content/plugins/asmasalonv1
php replace-emojis-cli.php --scan
```

### المشكلة: لا يتم استبدال الإيموجي في strings JavaScript
**الحل**: الأداة تركز على استبدال الإيموجي في template. للإيموجي في JavaScript strings، قد تحتاج لاستبدال يدوي.

### المشكلة: CIcon غير مستورد
**الحل**: الأداة تضيف الاستيراد تلقائياً. إذا لم يحدث ذلك، أضفه يدوياً:
```javascript
import { CIcon } from '@coreui/icons-vue';
```

## 📚 قائمة الإيموجي المدعومة

الأداة تدعم أكثر من 100 إيموجي. راجع ملف `replace-emojis-cli.php` للحصول على القائمة الكاملة.

## 🔗 روابط مفيدة

- [CoreUI Icons Documentation](https://icons.coreui.io/)
- [Vue.js Documentation](https://vuejs.org/)

## 📄 الترخيص

هذه الأداة جزء من مشروع Asmaa Salon Plugin.


